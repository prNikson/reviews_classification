max_len: 512
batch_size: 16
num_classes: 2

device: "cuda"
learning_rate: 0.00002
weight_decay: 0.01
num_epochs: 4
warmup_ratio: 0.1
gradient_clip: 1.0

wandb_project: "review_analysis"

train_dataset_path: "dataset/imdb_dataset.csv"
test_dataset_path: "dataset/hybrid_dataset.csv"

transformer_name: "bert-base-uncased"

models_save_path: "result_models"
